name: ojt_narrative_app
description: OJT Narrative Report Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.0.0
  
  # UI Components
  table_calendar: ^3.0.9
  
  # Date formatting
  intl: ^0.19.0
  
  # File picking
  file_picker: ^6.1.1
  
  # HTTP requests
  http: ^1.1.0
  
  # For web downloads
  universal_html: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true